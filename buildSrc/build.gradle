buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.3.10"
    }
}

repositories {
    google()
    jcenter()
}

apply from: file('common.gradle')

dependencies {
    deps.each {
        implementation it
    }
}
